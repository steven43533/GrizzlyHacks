{ 
    "title": "User", 
    "description": "JSON schema for the users.", 
    "type": "array",
    "required": [
        "firstName",
        "lastName",
        "email"
    ],
    "properties": { 
        "firstName": {
            "type": "string"
        },
        "lastName": {
            "type": "string"
        },
        "email": {
            "type": "string"
        },
        "registered": {
            "type": "boolean"
        }
    }
}