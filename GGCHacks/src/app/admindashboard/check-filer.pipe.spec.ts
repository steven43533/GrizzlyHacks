import { CheckFilerPipe } from './check-filer.pipe';

describe('CheckFilerPipe', () => {
  it('create an instance', () => {
    const pipe = new CheckFilerPipe();
    expect(pipe).toBeTruthy();
  });
});
